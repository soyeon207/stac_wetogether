<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.we_together">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".room_dialog"></activity>
        <activity android:name=".add_dialog" />
        <activity android:name=".inviteDialog" />
        <activity android:name=".Menu_community" />
        <activity android:name=".Menu_calendar" />
        <activity android:name=".Menu_share" />
        <activity android:name=".UploadMain" />
        <activity android:name=".Menu_home" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".placeActivity" />
        <activity
            android:name=".Splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinRoom"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".MakeRoom"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".Room"
            android:theme="@style/AppTheme.NoActionBar"
            ></activity>
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".join2"
            android:label="@string/title_activity_join2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity2" />
    </application>

</manifest>